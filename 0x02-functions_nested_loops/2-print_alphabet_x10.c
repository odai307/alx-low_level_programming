#include <stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0
 */
int main(void)
{
	int print_alphabet_x10(int n);
	int n;
	int m = 0;

	for ( n = 97; n < 123; n++)
	{
		while (m < 10)
		{
			m++;
		}
		putchar(n);
		}
	}
	putchar('\n');
	return (0);
}
